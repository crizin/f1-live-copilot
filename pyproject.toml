[project]
name = "f1-live-copilot"
version = "0.1.5"
description = "F1 Live Copilot — AI-powered real-time F1 race commentary"
requires-python = ">=3.10"
dependencies = [
    "websockets>=16.1.1",
    "requests>=2.31",
    "openai>=2.53.0",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.27",
]
